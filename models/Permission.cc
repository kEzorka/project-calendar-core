/**
 *
 *  Permission.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "Permission.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::project_calendar;

const std::string Permission::Cols::_key = "\"key\"";
const std::string Permission::Cols::_description = "\"description\"";
const std::string Permission::Cols::_is_global = "\"is_global\"";
const std::string Permission::primaryKeyName = "key";
const bool Permission::hasPrimaryKey = true;
const std::string Permission::tableName = "\"permission\"";

const std::vector<typename Permission::MetaData> Permission::metaData_={
{"key","std::string","text",0,0,1,1},
{"description","std::string","text",0,0,0,1},
{"is_global","bool","boolean",1,0,0,0}
};
const std::string &Permission::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
Permission::Permission(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
        if(!r["key"].isNull())
        {
            key_=std::make_shared<std::string>(r["key"].as<std::string>());
        }
        if(!r["description"].isNull())
        {
            description_=std::make_shared<std::string>(r["description"].as<std::string>());
        }
        if(!r["is_global"].isNull())
        {
            isGlobal_=std::make_shared<bool>(r["is_global"].as<bool>());
        }
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 3 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
        index = offset + 0;
        if(!r[index].isNull())
        {
            key_=std::make_shared<std::string>(r[index].as<std::string>());
        }
        index = offset + 1;
        if(!r[index].isNull())
        {
            description_=std::make_shared<std::string>(r[index].as<std::string>());
        }
        index = offset + 2;
        if(!r[index].isNull())
        {
            isGlobal_=std::make_shared<bool>(r[index].as<bool>());
        }
    }

}

Permission::Permission(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 3)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        dirtyFlag_[0] = true;
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            key_=std::make_shared<std::string>(pJson[pMasqueradingVector[0]].asString());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            description_=std::make_shared<std::string>(pJson[pMasqueradingVector[1]].asString());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            isGlobal_=std::make_shared<bool>(pJson[pMasqueradingVector[2]].asBool());
        }
    }
}

Permission::Permission(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("key"))
    {
        dirtyFlag_[0]=true;
        if(!pJson["key"].isNull())
        {
            key_=std::make_shared<std::string>(pJson["key"].asString());
        }
    }
    if(pJson.isMember("description"))
    {
        dirtyFlag_[1]=true;
        if(!pJson["description"].isNull())
        {
            description_=std::make_shared<std::string>(pJson["description"].asString());
        }
    }
    if(pJson.isMember("is_global"))
    {
        dirtyFlag_[2]=true;
        if(!pJson["is_global"].isNull())
        {
            isGlobal_=std::make_shared<bool>(pJson["is_global"].asBool());
        }
    }
}

void Permission::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 3)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            key_=std::make_shared<std::string>(pJson[pMasqueradingVector[0]].asString());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            description_=std::make_shared<std::string>(pJson[pMasqueradingVector[1]].asString());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            isGlobal_=std::make_shared<bool>(pJson[pMasqueradingVector[2]].asBool());
        }
    }
}

void Permission::updateByJson(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("key"))
    {
        if(!pJson["key"].isNull())
        {
            key_=std::make_shared<std::string>(pJson["key"].asString());
        }
    }
    if(pJson.isMember("description"))
    {
        dirtyFlag_[1] = true;
        if(!pJson["description"].isNull())
        {
            description_=std::make_shared<std::string>(pJson["description"].asString());
        }
    }
    if(pJson.isMember("is_global"))
    {
        dirtyFlag_[2] = true;
        if(!pJson["is_global"].isNull())
        {
            isGlobal_=std::make_shared<bool>(pJson["is_global"].asBool());
        }
    }
}

const std::string &Permission::getValueOfKey() const noexcept
{
    static const std::string defaultValue = std::string();
    if(key_)
        return *key_;
    return defaultValue;
}
const std::shared_ptr<std::string> &Permission::getKey() const noexcept
{
    return key_;
}
void Permission::setKey(const std::string &pKey) noexcept
{
    key_ = std::make_shared<std::string>(pKey);
    dirtyFlag_[0] = true;
}
void Permission::setKey(std::string &&pKey) noexcept
{
    key_ = std::make_shared<std::string>(std::move(pKey));
    dirtyFlag_[0] = true;
}
const typename Permission::PrimaryKeyType & Permission::getPrimaryKey() const
{
    assert(key_);
    return *key_;
}

const std::string &Permission::getValueOfDescription() const noexcept
{
    static const std::string defaultValue = std::string();
    if(description_)
        return *description_;
    return defaultValue;
}
const std::shared_ptr<std::string> &Permission::getDescription() const noexcept
{
    return description_;
}
void Permission::setDescription(const std::string &pDescription) noexcept
{
    description_ = std::make_shared<std::string>(pDescription);
    dirtyFlag_[1] = true;
}
void Permission::setDescription(std::string &&pDescription) noexcept
{
    description_ = std::make_shared<std::string>(std::move(pDescription));
    dirtyFlag_[1] = true;
}

const bool &Permission::getValueOfIsGlobal() const noexcept
{
    static const bool defaultValue = bool();
    if(isGlobal_)
        return *isGlobal_;
    return defaultValue;
}
const std::shared_ptr<bool> &Permission::getIsGlobal() const noexcept
{
    return isGlobal_;
}
void Permission::setIsGlobal(const bool &pIsGlobal) noexcept
{
    isGlobal_ = std::make_shared<bool>(pIsGlobal);
    dirtyFlag_[2] = true;
}
void Permission::setIsGlobalToNull() noexcept
{
    isGlobal_.reset();
    dirtyFlag_[2] = true;
}

void Permission::updateId(const uint64_t id)
{
}

const std::vector<std::string> &Permission::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
        "key",
        "description",
        "is_global"
    };
    return inCols;
}

void Permission::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[0])
    {
        if(getKey())
        {
            binder << getValueOfKey();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[1])
    {
        if(getDescription())
        {
            binder << getValueOfDescription();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getIsGlobal())
        {
            binder << getValueOfIsGlobal();
        }
        else
        {
            binder << nullptr;
        }
    }
}

const std::vector<std::string> Permission::updateColumns() const
{
    std::vector<std::string> ret;
    if(dirtyFlag_[0])
    {
        ret.push_back(getColumnName(0));
    }
    if(dirtyFlag_[1])
    {
        ret.push_back(getColumnName(1));
    }
    if(dirtyFlag_[2])
    {
        ret.push_back(getColumnName(2));
    }
    return ret;
}

void Permission::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[0])
    {
        if(getKey())
        {
            binder << getValueOfKey();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[1])
    {
        if(getDescription())
        {
            binder << getValueOfDescription();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getIsGlobal())
        {
            binder << getValueOfIsGlobal();
        }
        else
        {
            binder << nullptr;
        }
    }
}
Json::Value Permission::toJson() const
{
    Json::Value ret;
    if(getKey())
    {
        ret["key"]=getValueOfKey();
    }
    else
    {
        ret["key"]=Json::Value();
    }
    if(getDescription())
    {
        ret["description"]=getValueOfDescription();
    }
    else
    {
        ret["description"]=Json::Value();
    }
    if(getIsGlobal())
    {
        ret["is_global"]=getValueOfIsGlobal();
    }
    else
    {
        ret["is_global"]=Json::Value();
    }
    return ret;
}

Json::Value Permission::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 3)
    {
        if(!pMasqueradingVector[0].empty())
        {
            if(getKey())
            {
                ret[pMasqueradingVector[0]]=getValueOfKey();
            }
            else
            {
                ret[pMasqueradingVector[0]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[1].empty())
        {
            if(getDescription())
            {
                ret[pMasqueradingVector[1]]=getValueOfDescription();
            }
            else
            {
                ret[pMasqueradingVector[1]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[2].empty())
        {
            if(getIsGlobal())
            {
                ret[pMasqueradingVector[2]]=getValueOfIsGlobal();
            }
            else
            {
                ret[pMasqueradingVector[2]]=Json::Value();
            }
        }
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    if(getKey())
    {
        ret["key"]=getValueOfKey();
    }
    else
    {
        ret["key"]=Json::Value();
    }
    if(getDescription())
    {
        ret["description"]=getValueOfDescription();
    }
    else
    {
        ret["description"]=Json::Value();
    }
    if(getIsGlobal())
    {
        ret["is_global"]=getValueOfIsGlobal();
    }
    else
    {
        ret["is_global"]=Json::Value();
    }
    return ret;
}

bool Permission::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("key"))
    {
        if(!validJsonOfField(0, "key", pJson["key"], err, true))
            return false;
    }
    else
    {
        err="The key column cannot be null";
        return false;
    }
    if(pJson.isMember("description"))
    {
        if(!validJsonOfField(1, "description", pJson["description"], err, true))
            return false;
    }
    else
    {
        err="The description column cannot be null";
        return false;
    }
    if(pJson.isMember("is_global"))
    {
        if(!validJsonOfField(2, "is_global", pJson["is_global"], err, true))
            return false;
    }
    return true;
}
bool Permission::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                    const std::vector<std::string> &pMasqueradingVector,
                                                    std::string &err)
{
    if(pMasqueradingVector.size() != 3)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty())
      {
          if(pJson.isMember(pMasqueradingVector[0]))
          {
              if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[0] + " column cannot be null";
            return false;
        }
      }
      if(!pMasqueradingVector[1].empty())
      {
          if(pJson.isMember(pMasqueradingVector[1]))
          {
              if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[1] + " column cannot be null";
            return false;
        }
      }
      if(!pMasqueradingVector[2].empty())
      {
          if(pJson.isMember(pMasqueradingVector[2]))
          {
              if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, true))
                  return false;
          }
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Permission::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("key"))
    {
        if(!validJsonOfField(0, "key", pJson["key"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    if(pJson.isMember("description"))
    {
        if(!validJsonOfField(1, "description", pJson["description"], err, false))
            return false;
    }
    if(pJson.isMember("is_global"))
    {
        if(!validJsonOfField(2, "is_global", pJson["is_global"], err, false))
            return false;
    }
    return true;
}
bool Permission::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                                  const std::vector<std::string> &pMasqueradingVector,
                                                  std::string &err)
{
    if(pMasqueradingVector.size() != 3)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
      {
          if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
      if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
      {
          if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, false))
              return false;
      }
      if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
      {
          if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, false))
              return false;
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Permission::validJsonOfField(size_t index,
                                  const std::string &fieldName,
                                  const Json::Value &pJson,
                                  std::string &err,
                                  bool isForCreation)
{
    switch(index)
    {
        case 0:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 1:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 2:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isBool())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
